name: actions-ping

on:
  workflow_dispatch:

permissions:
  contents: read

jobs:
  ping:
    runs-on: ubuntu-24.04
    timeout-minutes: 2
    steps:
      - run: echo "ping ok $(date -u +%Y-%m-%dT%H:%M:%SZ)"
